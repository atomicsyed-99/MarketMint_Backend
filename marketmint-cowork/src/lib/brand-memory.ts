/**
 * @deprecated Use `buildBrandMemoryBlock` from `@/routes/chat/helpers` (single source of truth for chat).
 * Kept in sync for reference; chat route imports helpers only.
 */
export { buildBrandMemoryBlock } from "@/routes/chat/helpers";
